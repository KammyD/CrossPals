# 2026-09-14 工作日志

## 去水印 + 6 项版式/内容改动（CrossPals 网站）

### 1. 全站图片去掉 AI 水印（20 张）
- ImageGen 产出图右下角带固定水印「AI生成 / WORKBUDDY>」（右/下各留 6px，区域约 `x∈[w-124,w-6]`、`y∈[h-60,h-6]`）。
- 方案：纯 Pillow 脚本（`.workbuddy/tmp/dewatermark.py`，已沉淀到 skill `imagegen-watermark-removal`）——**谐波扩散补底（以框上方块做初值、四周边界为真实像素）+ 邻近块高频纹理注入 + 缝处 6px 渐隐**。0.6–1.0s/张，18 张全部处理完并拼图目视验收无水印残留。
- 原图备份在 `.workbuddy/tmp/img-orig/`。
- 新增 2 张客户头像 `public/img/avatar-nigeria.png`（尼日利亚女创业者）、`avatar-mexico.png`（墨西哥男创业者），同样已去水印。

### 2. 首页 Success Stories 留言加头像
`index.astro` / `es/index.astro` 的 `.testi__row` 两条 `.pq-sm` 留言上方各加 `<img class="pq-sm__avatar">`（56×56，`object-fit:cover; object-position:50% 24%` 让脸居中）。

### 3. Why CrossPals 右列垂直居中
`.why__grid` 的 `align-items:start` → **`center`**（en/es 首页）。

### 4. 首页 Resources 改瀑布流（消除 1688 那篇下面的留白）
原因：原布局左「精选卡（16/10 图）」+ 右「两小卡纵排」，左列高约 540px、右列约 1050px → 精选卡下方空出约 500px。
改法：**扁平化 DOM**（去掉 `.blog-prev__side` 包裹），`.blog-prev__grid` 改 `column-count:2`、`.bcard{display:block;break-inside:avoid;margin-bottom}`；精选卡图 `3/4`、小卡图 `16/9` → 两列高约 923 / 964，基本配平。

### 5. Blog 页改矩阵对齐
`blog/index.astro`：`.posts` 由 `column-count:3` 改回 `grid-template-columns:repeat(3,1fr); align-items:stretch`；删掉 `ratios` 数组，所有封面统一 `16/9`；卡片 flex 纵向 + `.pcard__foot{margin-top:auto}` → 同排等高、标题/脚注对齐。

### 6. Our Process / What We Stand For 内容居中
- `services.astro` / `es/services.astro`：`.tl-head{text-align:center}`，`.ledger-list{max-width:820px; margin:0 auto; text-align:left}`。
- `about.astro` / `es/about.astro`：该 section 的 `.section-hd` 内联样式改 `margin:0 auto var(--head-gap); text-align:center`，`.vals{max-width:820px; margin:0 auto; text-align:left}`。
（判据：整块居中、块内文本仍左对齐。）

### 7. Our Guarantees 图标下加配图
4 处（首页/服务页 × en/es）的 `.guarantee__item` 在 icon 与 h4 之间插入 `<ImgPlaceholder class="guarantee__img" ratio="3/2">`；新增每页 `guarImg` 映射 const：`trust→factory, inspection→inspection, payment→payment, shipping→shipping`（复用已有图，无新增生成）。`global.css` 加 `.guarantee__img{margin:0 0 var(--s4)}`。

### 8. 团队信息
`about.astro` / `es/about.astro`：`Wei Zhang, Head of Sourcing, Yiwu` → **`Xizeng, Founder & CEO, HeBei · BaoDing`**（bio 同步把 Yiwu 换成 HeBei · BaoDing）。用户只指了这一处，全站其他 Yiwu 提及（hero 图注、ticker「Yiwu → World」、首页 sourcing 描述、尼日利亚留言）**未动**，待确认。

- build 通过 15 页；产物校验：头像 2/2、保障配图 4×4、`blog-prev__grid{column-count:2}`、`.posts{grid repeat(3,1fr)}`、`why__grid{align-items:center}`、`vals/ledger-list{max-width:820px;margin:0 auto}`、about 页含 Xizeng/HeBei · BaoDing。

### 9. 收尾：全站图片转 WebP + Yiwu 统一改 HeBei · BaoDing
- **WebP 压缩**：`public/img/` 20 张照片 PNG → WebP（q82/method6），**32.2MB → 2.8MB**（dist 34MB → 3.7MB），1024px 尺寸不变。2 张 logo 保留 PNG（带 alpha，Logo.astro 引用）。原 PNG 归档到 `.workbuddy/tmp/img-png/`（去水印版）。
- **引用改写**：8 个页面 27 行 `'/img/'+X+'.png'` / `src="/img/*.png"` → `.webp`（脚本按「行内含 /img/」精确替换，`layouts/BaseLayout.astro` 的 favicon/og-image 未受影响）。产物 webp 引用 78 处、非 logo 的 png 引用 0。
- **`g-yiwu.webp` → `g-market.webp`**（about / es/about 的 `galImg` 映射同步），避免 URL 里留旧地名。
- **Yiwu 全部改为 HeBei · BaoDing**：首页/西语首页的 ① hero 图注「Yiwu International Trade Market, Zhejiang」→「HeBei · BaoDing, China」② 跑马灯「Yiwu → World / Yiwu → Mundo」→「HeBei · BaoDing → World / → Mundo」③ What We Do 的「networks in Yiwu, Guangzhou and Shenzhen」→「in HeBei · BaoDing, Guangzhou and Shenzhen」④ 尼日利亚留言「Got my Yiwu supplier in 48 hours」→「Got my supplier in HeBei · BaoDing within 48 hours」。博客正文那句事实描述改写为「China's small-commodity hubs — HeBei · BaoDing, Yiwu, and Guangzhou — supply…」（保留 Yiwu 作为市场名，句子仍成立），全站仅此一处保留 Yiwu。
- 预览已重启，4 个页面 + webp 均 200。

### 10. 上线缺陷收尾：死链清零（21 页）
用脚本扫 dist 全文 href 做**内链体检**，找出 8 类死链并全部修掉，复查 **死链 0**：
- **新建 `/privacy` `/terms` + `/es/privacy` `/es/terms`**：新增 `src/data/legal.ts`（英西双语法务文案，隐私政策 8 节 / 服务条款 10 节）+ `src/components/Legal.astro`（PageHero + 760px 居中正文列），4 个页面文件只是薄壳 `<Legal lang kind />`。**文案是通用模板，已告知用户上线前需律师复核**（公司主体/地址等占位）。
- **新建 `/es/blog`**（西语文章索引）：列表式（缩略图 8/5 + 分类 + 标题 + 阅读时长），分类翻西语，标题保留英文（文章正文是英文），顶部加一行说明「artículos en inglés」+ 底部 CTA。修掉了西语导航/页脚 Blog 链接的 404。
- **新建 `/404.astro`**：深绿底 404 页，英西双语（按 `getLangFromUrl` 判断），带回首页/联系我们 CTA 与邮箱。
- **语言下拉砍掉 fr/ar/pt**（`Header.astro` 的 `languages` 只剩 en/es），这三项原本点了就 404。
- **`robots.txt`** 原来指向不存在的 `sitemap-index.xml` → 新建 `src/pages/sitemap.xml.ts`（Astro 端点，静态产出 `dist/sitemap.xml`，20 条路由含 priority/lastmod，域名取 `Astro.site`），robots 改指 `sitemap.xml`。**新增页面时记得把路由补进 `sitemap.xml.ts` 的 routes**。
- **ContactForm 加邮件兜底**：表单下方加 `{Prefer email? / ¿Prefieres correo?} hello@crosspals.com`（mono 12px，朱红下划线）——在 web3forms key 未配置前仍能收到询盘。
- 仍需用户提供的真实信息：web3forms `access_key`、WhatsApp 号、LinkedIn/Instagram 主页、最终域名（`astro.config` 现为 `https://crosspals.com`）。

### 11. SEO / 性能补强（无需用户信息的部分）
- **BaseLayout 扩写**：canonical 已有，新增 **hreflang 三连**（en / es / x-default）+ `og:url` / `og:site_name` / `og:locale(+alternate)` / `og:type`（文章页自动 article）。hreflang 只在**双语都存在的路径**上输出：`bilingual = ['/','/services/','/about/','/contact/','/blog/','/privacy/','/terms/']`，博客文章页（西语无对应正文）**不输出** hreflang —— 避免指向不存在的版本。以后新增双语页要把它加进这个数组。
- **JSON-LD**：首页输出 `Organization + WebSite`（@graph），博客文章页输出 `Article`（BaseLayout 新增 `article={{ published, section }}` prop，由 `blog/[slug].astro` 传入 headline/datePublished/image/publisher）。
- **首屏图优先级**：`ImgPlaceholder` 新增 `priority` prop（`loading="eager"` + `fetchpriority="high"`，并补 `width/height` 属性），已用于首页/西语首页 hero、博客列表精选图、文章页头图（每页恰好 1 张），其余 14 张仍 lazy。
- **可访问性**：`<body>` 顶部加 `.skiplink`（Skip to content，聚焦才显形，样式在 global.css）指向 `<main id="main">`。
- **meta 审计**：20 个页面 title/description 全部唯一、无缺失。
- 复核：build 21 页通过，死链仍为 0。


### 12. 收尾一致性 & 最终验收
- **图片命名对齐人名**：`public/img/team-wei.webp` → `team-xizeng.webp`（姓名改 Xizeng 后旧文件名不一致），`about.astro` / `es/about.astro` 的 `teamImg` 映射同步改。
- **残留扫描**：`Wei`/`Zhang` 全站 0 处；`Yiwu` 仅剩 1 处（博客《5 Products That Are Cheaper in China》正文事实描述：HeBei · BaoDing, Yiwu, and Guangzhou）——有意保留，用户若要彻底去掉再改。
- **最终验收**：build 21 页通过；dist 3.8MB；内链 31 条 / 路由 22 个，**死链 0**（`/_astro/*.css` 与 `/favicon*.png` 曾被脚本误报，实际文件均存在）；引用的 22 张图片全部存在，**缺图 0**；sitemap 20 条 URL（404 页正确排除）；预览 9400 端口 10 个页面全部 200。
- **健壮性提醒**：内链体检脚本别只用 `glob('dist/**/*.html')` 收集路由，要把 `*.css` / `*.png` / `*.xml` 也算进 routes，否则会把静态资源误报成死链。脚本已固化为 `.workbuddy/tmp/linkcheck.py`。


### 13. 顶栏二次改版：深橄榄绿 →「左白 → 右黑」半透明渐变 + 滚动方向隐藏
- **背景换成两层交叉淡出的横向渐变**（写在 `Header.astro` 的 `.hdr` 上）：
  - 第 1 层白：`rgba(255,255,255,var(--hdr-w-a))` 从 0 保持到 `calc(--hdr-wz - 150px)`，到 `--hdr-wz` 淡出为 0；
  - 第 2 层黑：`rgba(10,13,8,0)` 从 `calc(--hdr-wz - 150px)` 起，到 `--hdr-wz` 达 `.62`，到 100% 达 `var(--hdr-k-a)`。
  - ⚠️ **不要**用一个 `linear-gradient(#fff → #000)`：alpha 也跟着插值，中间会糊出一条不透明灰带，必须拆两层交叉淡出。
- **`--hdr-wz`（白区右边界）= `max(210px, calc(50% - 420px))`**：`.container` 是 1180px 居中，用 `50% - 420px` 让白区右沿始终跟着 logo 走（logo 右沿 ≈ `50% - 449px`），宽屏不会脱开；窄屏被 `max(210px,…)` 兜住。移动端媒体查询里覆写为 `152px`（logo 同时降到 36px）。
- **文字分色**：左端白底 ⇒ 用**深色原版 logo**（`<Logo>` 的 `variant` 去掉，即 `logo-horizontal.png`）；右端黑区 ⇒ 导航/语言/汉堡仍用 `--hdr-fg`（米白）。nav 是 `margin-left:auto` 右对齐，任何宽度都落在黑区，所以两套颜色不会打架。
- **滚动方向隐藏**：`.hdr--hide { transform: translateY(-100%) }`；JS 在 `onScroll` 里用 `acc` 累计同方向位移，向下累计 >24px 收起、向上累计 >10px 滑出，`y ≤ 140px` 或菜单/语言面板打开时强制显示；缓动 `.46s cubic-bezier(.16,1,.3,1)`。因为顶栏是 fixed + 各页用 `padding-top: var(--hdr-h)` 补偿，隐藏不会引起布局跳动。
- `--hdr-bg` 保留（只有移动抽屉还用实色底）。

### 14. 真实站信息落地（用户提供：电话 / 域名 / 主体）
- **新增 `src/config/site.ts` —— 全站唯一来源**：`SITE_URL` / `PHONE{display,tel,wa}` / `EMAIL{en,es}` / `COMPANY{nameZh,nameEn,address{en,es,zh},addressParts}`。以后改电话、域名、主体名只改这一个文件。
  - 电话 **+86 151 0026 8521** → `tel:+8615100268521`，WhatsApp `https://wa.me/8615100268521`（替掉 4 处 `wa.me/yourwhatsapp`：Footer、contact en/es、index/es-index 的 `cta-final__note`）。
  - 主体：**`Baoding Zhihang Technology Co., Ltd.` / 保定职航科技有限公司**，地址 `Baoding, Hebei Province, China`（西语 `Baoding, provincia de Hebei, China`）。
- **最终域名 `https://www.crosspals.com`**（用户写的是 `http://www.CrossPals.com`，我规范成 **https** —— Cloudflare Pages 只走 https，canonical/sitemap 用 http 会被判不规范；要保留 http 说一声）：改了 `astro.config.mjs` 的 `site`、`BaseLayout` 的 `SITE_URL` 兜底、`sitemap.xml.ts` 兜底、`public/robots.txt`。⚠️ `astro.config.mjs` 是 .mjs，**import 不了 site.ts，域名存在两份，改时同步**。
- **结构化数据**：`Organization` 补 `legalName` / `telephone` / `address`（PostalAddress：Baoding / Hebei / CN）。
- **法务页**：`legal.ts` 新增 `legalEntity` 导出（主体名中英、注册地址、电话+邮箱），`Legal.astro` 在"最后更新"下方渲染成**朱红竖线信息块**；隐私政策 §1、服务条款 §1 都改写为"CrossPals 是 XXX 公司的商号"，联系段落补电话。
- **联系页**：新增 **Phone 卡片**（`tel:` 链接）与 **Who you're dealing with 主体块**（中英主体名 + 地址），英西各一份。
- **页脚**：社交栏 WhatsApp 接真实号（`target=_blank rel=noopener`）；联系区改成「电话 + 邮箱」两行（西语走 `hola@crosspals.com`）；品牌列底部加主体英文名 + 地址。
- **`linkcheck.py` 修好**：routes 改为遍历 `dist/**/*` 全部文件（`/index.html` 归一成目录），把 `/_astro/*.css`、`/favicon*.png` 算作真实路由 → 现在 **routes 79 / links 31 / 死链 0 / 缺图 0**，不再误报。
- 验收：build 21 页通过；预览 9400 端口 11 个页面全 200；`logo-horizontal.png`（深色版）已在顶栏、`wa.me/8615100268521` 已替换、canonical 为 `https://www.crosspals.com/`。

### 13. 顶栏渐变调优（分界线右移 + 过渡更柔）
- 用户反馈顶栏「左白→右黑」渐变的两色交界太硬、位置偏左。改 `src/styles/global.css` token：
  - `--hdr-wz: max(300px, calc(50% - 260px))`（原 `max(210px, calc(50% - 420px))`）→ 分界线整体右移 **+160px**（1920/1600/1440/1280 一致）。
  - 新增 `--hdr-fade: min(320px, calc(var(--hdr-wz) * .58))`（原来固定 150px 写死在 Header.astro）→ 交叉淡出宽度 1920 下 150→320px，且保证 wz 的 42% 仍是实心白（logo 不会被淡到）。
  - 移动端（≤900px）覆写 `--hdr-wz:200px; --hdr-fade:82px`。
- `Header.astro` 的 `.hdr` 背景改成**两层声明**：先给两停靠点兜底版，再给**缓动多停靠点版**（白层 .72/.28、黑层 .24/.66 中间点，黑层在 `wz + .4*fade` 处就到满值 → 右侧更匀）。若浏览器不支持 `calc()` 乘 alpha，后一条整体失效、兜底仍在。
- **坑**：`linear-gradient(#fff → #000)` 直接写会让 alpha 一起插值、中段糊出灰带；必须拆成白层/黑层两条交叉淡出。
- 构建 21 页通过，预览 9400 全部 200。

### 14. 顶栏渐变改为「白 → 页面主色墨绿」+ 整体更透
- 用户要求：渐变不要落到黑色，落到**页面主色调墨绿**；整体**更透明一些**。
- `src/components/Header.astro`：墨绿层颜色 `rgba(10,13,8,…)` → **`rgba(28,33,20,…)`**（= `--avo-950 #1c2114`，与 hero / PageHero 底色同源，右端像长在页面里）。用 `replace_all` 一次替掉 8 处停靠点。
- `src/styles/global.css` token：
  - `--hdr-w-a: .80 → .64`，`--hdr-k-a: .80 → .72`
  - 滚动态 `.hdr--solid`：`--hdr-w-a .98→.84`、`--hdr-k-a .92→.86`（**刻意不写 1.0**，留一丝透，顶栏才不像贴纸）
  - `--hdr-bg`（移动抽屉底色）`#1d2418 → #1c2114` 与主色统一
  - `--hdr-wz` / `--hdr-fade` 沿用上一版（分界线右移 + 缓动），未动
- **对比度核算**（改完自己验的）：米白导航 `#e8e5da` 压在 `.72` 墨绿上，即使底下是亮照片，对比仍 ≈ **7:1**（远超 AA）；左端深绿 logo（主色采样约 `(50,90,30)`）压在 `.64` 白层上 ≈ **3.6:1**，图形类 3:1 门槛通过但偏软 —— 若用户反馈 logo 发虚，把 `--hdr-w-a` 提到 `.72` 即可。
- 构建 21 页通过；`rgba(10,13,8` 在 dist 中 0 处残留；预览 9400 全部 200。

### 15. 移动端适配（用户指令：不改 PC 端样式，只做 responsive）
- **背景**：用户觉得整站不移动端适配。实证审计发现——站点其实已大面积响应式（顶栏抽屉、Footer、services/about/contact/blog/Legal 页均有 `@media` 塌陷规则，经 21 页 × 320~1280px 全量测量**均零横向溢出**）。**唯一真缺陷**：英文/西语首页的 **WHY 区块 `.why__grid`（两栏 `1fr 1.35fr`）在 ≤860px 从不塌陷** → 右栏（No Language Barrier / Real Accountability 等）被裁切。
- **审计方法**：`playwright-core` + 系统 Edge 无头渲染，测量 `scrollWidth - clientWidth` 判定横向溢出；逐元素 `getBoundingClientRect()` 定位 offenders（排除 `.ticker` 宽滚动条伪阳性）。脚本存 `.workbuddy/tmp/`（非交付物）。
- **证据**：修复前仅 `/` 与 `/es/` 在 320/360/375/390/414/430px 溢出（153/113/98/83/59/43px）；其余 19 页全零。
- **修复**（仅动 `src/pages/index.astro` + `src/pages/es/index.astro` 末尾响应式块，**英西镜像同步**）：
  - `@media(max-width:860px)` 内补 `.why__grid{grid-template-columns:1fr;gap:var(--s6);}`（≤860 改单列）。
  - 新增 `@media(max-width:560px)`：`.why-item` 索引列 64→44px、`.blog-prev__head` 标题与按钮上下堆叠、`.masthead` 缩字号+隐藏中线。
  - （既有 640px 块已有 svc-row/manifest/cta-final/quick-form 塌陷，未动）。
- **验收**：`npm run build` 21 页通过；`overflow-all.cjs` 复测 = `ALL CLEAN — no overflow at any tested width/page`（320→768 全覆盖）。`verify-desktop.cjs` 确认 PC 零改动：1280px 下 `.why__grid` 仍两栏（461px+623px），390/360px 才单列。**PC 端样式完全未动。**
- 对比图产物：`mobile_why_before_after.png` / `mobile_site_contact_sheet.png`（`.workbuddy/tmp/shots/`，模型无法看图，仅归档）。

### 附：仍 pending 的历史遗留（非本轮范围，待用户给真实信息）
- `ContactForm.astro` 的 web3forms `access_key` 仍是占位 `YOUR_WEB3FORMS_KEY`（表单会提交失败，已加 `hello@crosspals.com` 邮件兜底）。
- 页脚 LinkedIn / Instagram 仍指向 `#`（WhatsApp 已接真实号）。

### 16. FBA 定位大改版（首页 / Services / About / 新 Case Studies / Blog 方向）
- **WhatsApp**：确认已是 `https://wa.me/8615100268521`（`src/config/site.ts` 的 `PHONE.wa`，格式正确：无 `+`、无空格），无需改动。
- **首页 `src/pages/index.astro` 重写**：
  - Hero 改 "The sourcing partner Amazon sellers trust — quality inspection included." + 副标 "We source, inspect, and prep your products for FBA…"；双 CTA `Get a free quote` / `See how it works`。
  - 客户区：国家旗帜 → **8 个品类场景标识**（复用 `.regionwall`）+ 指标条 `.trustmetrics`（47 / $2.3M+ / 98%）。
  - 新增 **`.scen` 三场景区块**（用户给的三条 FBA 卖家原话 + 对应服务）。
  - 新增 **`.inspect` 独立质检模块**（5 项标准 + `.qcreport` 样例报告，标注 client/supplier masked）。
  - 服务列表 4 项各加 `.price-note` 定价说明。
  - 证言改 FBA 格式；精选条 `.pullquote--feat` 文本左、头像右（响应 ≤640 头像移到下方）。
  - 新增 **`.pricing` 独立定价区块**（`.pricetable` + `.price-example` $1,000 示例）。
- **Services `src/pages/services.astro` 重构成两层**：核心包 `FBA Sourcing & Inspection`（5 步）+ 4 项附加服务（私标包装/产品摄影/1688 代购/仓储转运，每项「问题·流程·收费逻辑」）+ 独立定价区块 + FAQ 改写 + 内联 $1,000 示例。
- **About `src/pages/about.astro` 重建**：真实创始人故事（河北保定/大学老师/带学生实训起步/3 年 40+ 单，署名 "You deal with me — not a ticket queue."）；指标改真实（40+/12/3yrs/100%）；新增 **`.tp` Trustpilot 区块**（占位链接 `https://www.trustpilot.com/review/crosspals.com`，代码内注明注册后替换）；**删除 3 位虚构团队成员**。
- **新增 Case Studies**：`src/pages/case-studies.astro` + `src/pages/es/case-studies.astro`；4 个匿名案例（背景/问题/介入/结果）。导航加 `nav_cases`（`en.ts`/`es.ts` + `Header.astro` nav 数组），页脚 Company 列加 "Case Studies / Casos de Éxito"。展示图用现有 `blog-*.webp`。
- **Blog 方向改 FBA**：`blog/[slug].astro` 的 6 篇泛采购文章换成 **4 篇 FBA/Shopify 选题**（FBA 拒收 / 质检报告看什么 / 1688 vs Alibaba / 出货前 FBA-ready），每篇结尾 CTA → `/contact`；`blog/index.astro`、`es/index.astro`、`es/blog.astro` 的 slug 同步；`sitemap.xml.ts` 补 `/case-studies/`、`/es/case-studies/` 与新 4 个 blog 路由。
- **验收**：内链体检（`linkcheck.py`）**88 路由 / 23 内链 / 死链 0 / 缺图 0**；元素级横向溢出审计（135 项：15 页 × 9 宽度）**仅 `/about` 在 320px 溢出 10px**（Trustpilot 长按钮撑宽）→ 加 ≤540px `.tp` 收边距 + 按钮换行，复测 CLEAN。
- **⚠️ 重大发现：`npm run build` 在 Git Bash 下会静默丢 CSS**（全站无样式，21 页 HTML 全部没有 `<link rel=stylesheet>`），且偶发 `No cached compile metadata found for ...?astro&type=style` 硬报错。用 **PowerShell（`D:\` 大写盘符）构建则正常**（`dist/index.html` 有 2 个 stylesheet 链接；21/21 页带样式）。根因是 Windows 盘符大小写（`d:` vs `D:`）导致 Vite 样式模块缓存键未命中。**dev 模式不受影响**（Vite 直接注入 `<style>`），所以只跑 dev 预览发现不了。已写入 MEMORY.md。

## 16. Success Stories 三张客户头像去重（用户报「三张照片重复」）
- **根因**：`src/pages/index.astro` 证言数组里 `avatar-nigeria.webp` 被用了两次 —— 精选证言 Marcus T.（`testiFeatured.avatar`）与小卡 David O.（`testiSmall[1].avatar`）指向同一文件，所以三个人只有两张脸。
- **修复**：为 David O.（UK · Home & Garden）生成专属头像 `public/img/avatar-uk.webp`，`testiSmall[1].avatar` 改指向它；同时把三处 `alt` 从重复的 "Amazon seller portrait" 改成 `${name} — Amazon seller portrait`（a11y 去重）。
- **新头像生产流程（可复用，见 skill 改进建议）**：ImageGen 生成 `1024x1536` → **顶部对齐裁成 `1024x1280`**（与 `avatar-nigeria/mexico` 同规格，这样现有 CSS `object-position:50% 22%/24%` 的取景不变）→ `ImageEnhance.Brightness` 调亮度对齐兄弟图（首版偏暗 lum 63 → 重生成亮调版 lum 181 → ×0.83 落在 **144**，最终 RGB 均值 (150,143,136)，正好夹在 nigeria(163,156,147) 与 mexico(133,122,110) 之间）→ 存 `webp q82 method=6`（114KB）。
- **水印核查**：本次两张生成图右下角均**无水印**（判定法：右下 170×h*0.085 框内 near-white 计数 0、边缘均值 10.3 vs 对照块 19.1）。亮底图上白字水印不可靠，改用「边缘/标准差对比」而非肉眼判色。
- **验收**：`/` 与 `/es/` 在 320–1280px 共 18 项横向溢出**全 0**；构建后 `dist/index.html` 三张头像 `avatar-nigeria / avatar-mexico / avatar-uk` **各出现 1 次**；浏览器实测 naturalWidth 均 1024x1280 且 loaded；两两像素差异 49–55（相距甚远，非近重复）；21/21 页带 CSS。
- **遗留（下次可提）**：`src/pages/es/about.astro` **仍保留 3 位虚构团队成员**（`teamImg = {301:'team-xizeng',302:'team-amara',303:'team-carlos'}` + `.team-card`），英文版已删。若要彻底对齐，需把西语 About 也改成创始人故事版（顺带可让 `team-amara/carlos` 两张闲置人像素材退出使用）。

## 17. Our Guarantees 区块拆分：首页只给承诺，Services 页展开成四个模块
**起因**：用户指出首页与 Services 的 Our Guarantees 是重复区块，且「图片需要是不同的图片，尺寸大小要一致」。

- **首页 `index.astro`**：区块保留，四条文案压到一句「只讲结果」——`We vet every supplier before you pay a cent.` / `Photo report + pass/fail before your money moves.` / `We hold funds until milestones clear. No wires to strangers.` / `Tracking and goods — not paperwork.`；标题 `What You Get, Every Order` 不动；末尾新增 `.guar-lede`（套 `.lede-row`）指向 `/services#guarantees`（"See how it works →"）。**首页四张配图未换**（factory/inspection/payment/shipping）。
- **Services `services.astro`**：整块换成 `id="guarantees"` 的「Our Guarantees, In Full / What Each Promise *Actually Means*」，四个 `.gmod` 展开模块，每个含：`how it works` 流程（步数 + 每步耗时：`Day 1–2` / `Half a day, typical` / `Air 5–8 days · Sea 25–35 days` 等）+ 检查清单（QC 那份就是 外观/尺寸/功能/包装/FBA标签 五项，`✓` 前缀）+ 底部 `If this fails` 兜底承诺。数据在 `guaranteeModules`（`GuaranteeModule` 类型）；老的 `guarantees` 数组与 `guarImg` 已删除。
- **图片不重复**：Services 四张 = `g-market`（供应商核实）/ `g-production`（QC）/ **`payment-escrow`（新生成，1024×1280，锁+封箱+卡，专为「资金托管」）** / `g-packing`（DDP），与首页四张 **完全不重叠**，也都不是 Services 页附加服务在用的 blog-packaging/blog-products/sourcing-1688/shipping。渲染统一 `ratio="4/5"`，实测 1280px 四张全是 **318×398**、390px 全是 268×335。
- **⚠️ 顺带修掉一个真缺陷：`.guarantee` 四张配图原本尺寸不一致**（首页实测 258×172 / 234×156 / 234×156 / 259×173）。两个根因：① 首/末列外侧 padding 为 0 → 内容宽出 `var(--s5)`；② 末列 `border-right:none` 导致它比别的列多 1px 内容宽。修法（写进 `global.css`）：`.guarantee__item:first-child/last-child .guarantee__img{max-width:calc(100% - var(--s5))}` + 末列改 `border-right-color:transparent`（保留 1px 占位）+ ≤620px 单列时 `max-width:none`。修后首页 1280/1024/820/620/430/320px **全部等尺寸**，且 `overflow` 全 0。
- **验收**：12 个页面 × 8 宽度 = 96 项横向溢出全 0；`linkcheck.py` 81 路由 / 31 内链 / 死链 0 / 缺图 0。ES 页的旧 guarantee 网格也自动获得等尺寸修复（未改其文案）。
- **遗留**：`es/services.astro` 与 `es/index.astro` 的 Our Guarantees 仍是旧版泛文案（与英文 FBA 版未同步，属既有项）。

## 18. 博客 + 案例改为 md 文件驱动（Content Collections），案例新增配图与详情页
**起因**：用户要求「blog 和 case studies 应该做成可以添加内容的方式，内容用 md 文件表示；case studies 的案例要配图，并且每条内容右下角有了解详情，然后进入详情页查看」。

- **新建 Content Collections 地基**：
  - `src/content/config.ts` —— 两个 collection：
    - `blog`：`title / description / category / date / readTime / cover / featured / draft` + 可选 `titleEs / descriptionEs / categoryEs`
    - `caseStudies`：`title / client / summary / problem / action / results[{v,l}] / cover / date / order / draft` + 可选 `titleEs / clientEs / summaryEs / problemEs / actionEs / resultsEs[]`
  - **日期统一用 `z.string()` 存 `'YYYY-MM-DD'` 带引号字符串**（不加引号的 YAML 日期会被解析成 Date，产生时区偏移）；显示时用 `new Date(d + 'T00:00:00')` 本地解析保证不跨天。
  - `*Es` 字段是**可选西语覆盖**，缺省回退英文 —— 以后要翻译只需在 md 里补字段，不用改代码。
  - **新建 `tsconfig.json`**（`extends: astro/tsconfigs/base`）—— 之前没有，`astro:content` 类型需要它。
- **内容迁移（不新增文章，只换载体）**：
  - 4 篇博客正文从 `blog/[slug].astro` 的**内联 HTML 模板串** → `src/content/blog/*.md`（`**加粗**`、`[文本](/contact)`、`##` 标题），内容一字未改。
  - 4 个案例 → `src/content/case-studies/*.md`，结构化字段进 frontmatter，正文（Body）**新写了完整案例叙述**：The situation / What we did / The result / 小结，含具体质检细节（AQL 2.5、按缺陷等级计数、里程碑放款四段等）。
- **页面改造**：
  - `blog/index.astro`、`blog/[slug].astro`、`es/blog.astro` 全部改 `getCollection` + 排序（日期倒序）+ `draft` 过滤；详情页正文用 `<div class="prose"><Content /></div>`（**scoped style 对 `<Content />` 必须用 `:global(...)`**，原代码已是这样）。
  - **`case-studies.astro` 单页拆成目录**：`case-studies/index.astro`（卡片网格：4:3 配图 + 编号/客户背景 + 标题 + 摘要 + 3 个结果指标 + 底部条**右侧 `Learn more →`**）与 `case-studies/[slug].astro`（21:9 头图 + 编号/客户 + 结果指标条 + markdown 正文 + 侧栏 CTA + 底部「其他案例」3 卡 + 深色 CTA）。**旧的 `src/pages/case-studies.astro` 与 `src/pages/es/case-studies.astro` 已删除**（否则与目录路由冲突）。
  - 西语镜像：`es/case-studies/index.astro`（用 `*Es` 字段）+ `es/case-studies/[slug].astro`（西语 UI + 西语 results + 「El problema / Lo que hicimos」西语摘要 + 英文正文，顶部一行西语小注）。
- **sitemap 动态化**：`sitemap.xml.ts` 改为 `getCollection` 读博客/案例，**新增 md 自动进 sitemap**（28 urls）。
- **BaseLayout hreflang**：`bilingual` 数组加 `/case-studies/`；新增动态判定 `isCaseDetail = /^\/case-studies\/[^/]+\/$/.test(pathNoLang)`，让案例详情页 `/case-studies/<slug>/` ↔ `/es/case-studies/<slug>/` 正确互指（blog 详情页无西语版，故不加）。
- **4 张案例配图（新生成）**：`case-kitchen-qc` / `case-pet-fba` / `case-supplier-verify` / `case-relabel`，`1280×960`（4:3），走 skill 去水印（4 张右下角都原本带水印，检测到文字字形后跑脚本清除），再按亮度向 100 线性校正（增益限 0.85–1.25）转 webp q82（65–103KB）。与站内其他图不重复。
- **验收**：构建 **29 页**（原 21 + 4 英文案例详情 + 4 西语案例详情）；**29/29 页带 CSS**；`linkcheck.py` **103 路由 / 41 内链 / 死链 0 / 缺图 0**；移动端横向溢出 **19 页 × 6 宽度 = 114 项，全 0**。
- **Astro 4.16 API 备忘**：取内容用 `import { getCollection } from 'astro:content'`，渲染用 `const { Content } = await entry.render();`（Astro 5 改成 `render(entry)`）；`entry.slug` 即文件名（不含扩展名），也就是 URL。

## 收尾：案例卡头部改竖排 + 顶栏渐变重做（用户截图反馈）
- **问题 1｜标签与序号同行**：`case-studies/index.astro` 的 `.ccard__head` 原是 `flex;align-items:baseline;flex-wrap:wrap`，标签长的（"AMAZON SELLER · UNITED STATES · KITCHEN & DINING"）被迫折行、短的（"AMAZON FBA · SPAIN · PET SUPPLIES"）留在 02 右边，两张卡高度参差。改 `display:block` + `.ccard__n{display:block}` + `.ccard__client{display:block;margin-top:9px}`，序号独占一行、标签必另起一行。**西语镜像同步改**。
- **问题 2｜顶栏渐变"太突变"**：老写法是"实心白平台 + 一段近似线性过渡"（`--hdr-wz`/`--hdr-fade`），平台尽头 x≈130（1000px 视口）出现一道**肉眼可见的硬拐点**，且过渡只铺 250px 左右。
  重做为 **一个连续坡**：`--hdr-wp`（白层满格终点 ≈ logo 右缘+30px，保证 logo 亮底）+ `--hdr-wf`（收尾长度）＋ `--hdr-kz`（墨绿层铺满处 ≈ 首个导航项左缘 `50% - 277px`），白层用 **f(t)=1-smoothstep(t)^0.6**（首尾斜率均为 0，无台阶）11 个停靠点铺曲率；墨绿层用 smoothstep 9 个停靠点。三个变量都随视口 clamp 缩放，≤900px 另有移动端覆盖。
  中途踩坑：第一版让白层从 x=0 就开始衰减，**1920px 下 logo 掉进灰底几乎看不见**（logo 离左边 394px）——所以必须有 `--hdr-wp` 这个满格段。
- **验收**：构建通过、CSS 链接正常；`linkcheck.py` 103 路由/死链 0/缺图 0；`overflow-all.cjs` 全清；1920/1440/1000/390 四档顶栏（页顶 + 滚动后）与 990px 两列案例卡均截图比对，硬拐点消失、logo 恒在亮底。

## 西语站（es/）内容全面补齐（2026-09-14 晚）
**页面重写（结构对齐英文版）**
- `es/services.astro`：旧「6 项泛采购服务 + 4 条短保证」→ 英文版新结构：核心包（FBA Sourcing & Inspection，5 步）+ 4 个附加服务（品牌/摄影/1688/仓储）+ 独立定价区 + `#guarantees` 四展开模块（流程/清单/If this fails）+ 1.000 $ 示例 + 6 条新 FAQ。样式整块从英文版复制。
- `es/about.astro`：删 Amara Osei / Carlos Mendez 两个虚构成员与 `team-wide` 团队图，改创始人第一人称故事（保定·大学老师·3 年·40+ 单）+ 真实指标（40+/12/3 años/100%）+ Trustpilot 区块 + 4 价值观（第 3 条改「Hecho para Vendedores FBA」）。
- `es/index.astro`：Hero 换 FBA 定位文案 + 四步索引（Buscar/Inspeccionar/Preparar FBA/Entregar）；ticker 换品类关键词；国家旗帜墙 → 8 个品类场景 + 指标条（47/$2.3M+/98%）；服务项加 `price-note` 定价；新增三 FBA 场景模块 `.scen`、独立质检模块 `.inspect` + 样例报告 `.qcreport`、独立定价区；证言换 FBA 三头像（nigeria/mexico/uk 不重复）；Guarantees 砍短 + `.lede-row` 跳 `/es/services#guarantees`。

**西语正文（新增 8 个 md + 2 个 collection）**
- 新增 `src/content/blog-es/`（4 篇）与 `src/content/case-studies-es/`（4 篇），由两个子代理并行翻译；frontmatter 只保留西语字段（无 `*Es`），`date/cover/order` 与英文版严格一致，案例 `results[].v` 里的英文（"18 days"）也译成西语。
- `src/content/config.ts` 新增 `blogEs` / `caseStudiesEs` 两个镜像 collection。
- 新建 `src/pages/es/blog/[slug].astro`（西语博客详情页，带「Sigue leyendo」相关阅读）；`es/case-studies/[slug].astro` 与列表页、 `es/blog.astro` 都改为「-es 全文 → `*Es` 字段 → 英文」三级回退。
- `BaseLayout`：`isBlogDetail` 加入 hreflang 判定；默认 description / ORG.description / areaServed / skiplink 按 `lang` 西语化。`sitemap.xml.ts` 补 4 条 `/es/blog/<slug>/`。i18n 两侧 `footer_tagline` 换成 FBA 定位。

**坑**：`getCollection('caseStudies')`（camelCase）静默返回空 → 西语案例详情页消失（33→29 页，无报错）。必须用连字符名。首次构建后西语博客正文也没接上（同一个原因）。

**验收**：33 页全带 CSS；`linkcheck.py` 112 路由/46 内链/死链 0/缺图 0；`overflow-all.cjs` 更新为 33 页 × 9 宽度 = 297 项全 0 溢出；西语页源码中文残留 0；西语博客/案例正文英文残留 0。预览服务 http://localhost:8088 。
