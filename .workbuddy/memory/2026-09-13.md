# 2026-09-13

## 反 AI 设计重做（anti-ai-design.skill）收尾
- 用桌面 `anti-ai-design.skill`（基于 Anshu Chimala × Double Diamond 反 AI 委员会审美方法论）重做 CrossPals 全站布局，方向 "Trans-Pacific Ledger / 跨太平洋贸易手册"。
- 完成 `Footer.astro`：社交图标由圆形改锐角方块、hover 改朱红 `--seal`；列标题加朱红发丝线；contact 链接改 seal-soft+mono；bottom bar 去 emoji 改 "A CrossPals Trade Ledger · Est. MMXXVI" 刊尾体。
- 最终 `npm run build` 通过：15 页（含 es/index/services/about/contact 四页）无报错。
- 全站已落地：刊头 masthead、编辑式非对称 Hero（图注+印章）、ticker 跑马灯、目录式编号服务、账本式流程、大 pull-quote 案例、印章 manifesto CTA、文字 wordmark Logo、Fraunces+Instrument Sans+DM Mono 字体、锐角+发丝线+刊边批注。
- 设计 token 集中在 `src/styles/global.css`；ES 页自带 scoped 样式副本（Astro 作用域隔离）。

## 遗留（非本次布局范围，可选）
- `ContactForm` web3forms `access_key` 仍为占位 `YOUR_WEB3FORMS_KEY`；WhatsApp `wa.me/yourwhatsapp` 占位；`/privacy` `/terms` 死链；Header 语言下拉 fr/ar/pt 404；Footer 社交 LinkedIn/Instagram 指向 `#`；favicon 待补。

## 区块高度压缩 + 应用《网站基础选型样式模板集》
- 用户反馈"区块高度太高、每屏信息量少"。读桌面 `websit prompt/样式集/网站基础选型样式模板集.pdf`（18 页纯图片 PDF，PyMuPDF 渲染成 PNG 逐页看）——共 27 结构分 8 类：截图样式/Hero/Banner/Section/导航/信任证明/作品媒体/转化疑虑/页面组合。
- **压缩垂直节奏（global.css）**：新增 token `--section-y:56px`（原 96）、`--section-y-sm:40px`、`--head-gap:36px`；`.section` 96→56；h1 max 76→64、h2 56→46；p line-height 1.78→1.68。hero `100vh`→`min(78vh,680px)`；PageHero 144/96→112/56。
- **新增模板原语（global.css）**：`.announce`（公告提醒条）、`.statband`（数据证明：大号数字+事实列表）、`.guarantee`（保障模块网格）、`.banner-cta`（通栏行动）、`.regionwall`（地区/客户墙）、`.stepsline`、`.bento`。
- **英文首页**新增：顶部公告条、地区墙、数据证明 Section、通栏行动 Banner、保障模块网格；服务页加保障网格；全部内页 + ES 四页同步压缩。
- 最终 build 通过：15 页无报错。`final answer` 已用 present_files 展示 dist 四页。
- 注意：`banner-cta` 用 `.banner-cta__inner`（container 内 flex），不是 band 本身 flex。

## 顶栏/底栏改米白色
- 用户要求 Header（顶栏）与 Footer（底栏）换成米白色。米白映射到 `--warm-100:#f4ede0`（羊皮纸/cream）。
- `Header.astro`：`.hdr` 渐变遮罩→实色 `--warm-100` + 发丝线；导航/语言按钮/下拉面板/汉堡/drawer 全翻深色文字（`avo-800/950`、`text-muted`）；Logo 改 `variant="dark"`；CTA 朱红按钮保留。
- `Footer.astro`：`.footer` 背景 `avo-950`→`warm-100`，文字全翻深绿/暖灰；社交图标边框 `border-light`、hover 朱红；Logo 改 `variant="dark"`。
- 两组件被英/西全页共用，无需单独改 ES 页。build 通过 15 页。

## 全站区块高度二次压缩（用户强调不止 home）+ 换真实 logo
- 用户澄清：压缩范围是**所有区块高度过高的页面**，不只首页。复查发现漏掉的是 **blog 两个页面**（此前未纳入 v5 重做，仍是圆角旧样式）。
- `blog/index.astro`：精选卡由「全宽 16:7 大图」→ **编辑式左右分栏**（图 4:3 左 / 文右），单卡高度约减半；文章卡 16/9→2:1；`margin-bottom` s8→s6；分类按钮 `--rfull`→锐角；卡片去圆角/去 translateY 阴影 hover 改描边。
- `blog/[slug].astro`：头图 16:6→21:9 **且加 `max-height:44vh`+`overflow:hidden` 限幅**（全出血大图是最高的单块）；`.article-grid` gap s8→s6、侧栏 300→280px；正文 17px/1.85→16.5px/1.72，p 间距 s5→s4；h2 margin s8→s6、padding-top s6→s5，顶线改朱红；aside 卡片去圆角、`avo-700`→`avo-950`+朱红左边线。
- `Footer.astro`：`.footer__top` padding s9/s8→s7/s6、gap s8→s6；h5 margin s5→s4；bar padding 收紧。
- `global.css`：`.statband` gap s8→s6；**修 bug**——移动端 `.banner-cta` 应改为 `.banner-cta__inner` 才生效（原选择器错了，手机端通栏 Banner 一直没堆叠）。
- 移动端 hero padding 120/84px → 统一 100px（index + es/index）。
- **换真实 logo**（用户提供）：源 `D:\BaiduSyncdisk\项目2  CrossPals出海创业\Websit\CrossPals.com\`
  - `crosspals(1).png`（531×221，横版）→ `public/img/logo-horizontal.png`，**顶栏**
  - `crosspals (5).png`（1000×1000，竖版：鸟在上·字标在下）→ `public/img/logo-stacked.png`，**底栏**
  - 两张都是 RGBA 透明底。**用 Pillow 按 alpha bbox 自动裁掉透明边距**后：横版 509×208（aspect 2.447）、竖版 954×826（aspect 1.155）——裁完尺寸才对得上视觉比例。
  - `Logo.astro` 重写：props 改 `layout('horizontal'|'stacked') + size`，**height 驱动**（`--logo-h` + `width:auto`），顶栏 36px / 底栏 88px。删掉旧 `variant` 与文字 wordmark。
  - `Header.astro` 顺手锐角化残留圆角（nav link 6→0、lang btn 6→0、dropdown 12→0、burger 6→0、drawer lang item rfull→0、drawer cta 8→0）。
- **品牌资产补齐**（顺手修 404）：`BaseLayout.astro` 原本 `<link rel="icon" href="/favicon.svg">` 但 public 里**没有这个文件** → 改成 PNG favicon 三件套。用竖版 logo 的「墨迹行分带」切出上半带（鸟）补成方形画布：`public/favicon.png`(512) / `favicon-32.png` / `apple-touch-icon.png`(180)；另用横版 logo + 米白底生成 `public/og-image.png`(1200×630，须 convert RGB 否则社交端黑底)。同时补 og:image / twitter:card / theme-color。
- 最终 build 通过：15 页；产物内已确认引用 `logo-horizontal.png` `logo-stacked.png` `favicon.png` `apple-touch-icon.png` `og-image.png`。
- 沉淀 skill：`~/.workbuddy/skills/brand-asset-pipeline/SKILL.md`（logo→裁边距/favicon/OG 全流程）。


## 顶栏纯白 + 导航加粗加大 + CTA 移到文字一侧 + 删旧资产
- **顶栏改纯白**：`Header.astro` 的 `.hdr` / `.hdr--solid` 背景 `--warm-100`→`#fff`；顺带同翻 `.lang__dropdown` 面板与 `.drawer`（移动抽屉）背景为 `#fff`。底栏 Footer 仍保留米白（用户只要求顶栏改白）。
- **导航加粗加大 + 左移**：`.hdr__link` font-size 15→**17px**、font-weight 400→**600**（active 500→700）、padding 8/14→8/15、active 圆点 4→5px；`.hdr__nav` `margin-right` `--s5`(24)→**`--s7`(48)** 把整组导航往左推；CTA `.hdr__cta` 14→15px、padding 10/20→11/22 保持视觉平衡。新增 **1120–901px 中间断点**（字号回落 15.5px、padding 11px、margin-right `--s4`），避免粗体导航在平板宽度溢出。
- **删旧资产**：`public/img/logo-light.png`（63KB，已无引用）已删除，`dist/img/` 同步清掉；public/img 现只剩 `logo-horizontal.png` / `logo-stacked.png`。
- **新增全局原语 `.lede-row`（global.css）**：`display:flex; align-items:center; justify-content:space-between; gap:var(--s5); flex-wrap:wrap`，`>p/.lede-row__text` 为 `flex:1 1 240px; margin:0`，`>a/.btn` 为 `flex:0 0 auto`；≤640px 按钮换行并 `width:100%` 居中。**用途：正文与 CTA 并排——按钮永远贴在文字一侧，不再单独占一行**。
- **应用到 4 个文件（按钮从「图片下方」移到「描述文字右侧」）**：
  - `services.astro` / `es/services.astro`：6 行 `.svc-row__meta` 内，`<p>{s.desc}</p>` + `<a class="btn">` 包进 `.lede-row`，`ImgPlaceholder` 移到其后；页面 CSS `.svc-row__meta > p`→`.svc-row__meta p`，并加 `.svc-row__meta .lede-row{gap:var(--s4);margin-bottom:var(--s1)}`。
  - `index.astro` / `es/index.astro` 两处：①「服务列表」`.svc-row__body` 的 `Learn more →` / `Más información →` 移入 `.lede-row` 与 `{s.body}` 并排；②「Why CrossPals」`.why__left` 的 `Start for Free` / `Empieza Gratis` 移到段落右侧（在 `ImgPlaceholder` 之前），段落 16px/1.8→15px/1.72 以适配变窄的栏宽。
  - 两处 CSS 同步：`.svc-row__body p{...;margin:0;max-width:520px}`（原 `margin:var(--s2) 0 var(--s3)`），并加 `.svc-row__body .lede-row{margin-top:var(--s2)}`。
- **未动**（窄栏场景，并排会挤压；如需再说）：`index.astro` 的 `.testi__stat` 统计块（`p`+`Our Story →`）、`blog/[slug].astro` 侧栏 `Free Quote`、`blog/index.astro` 订阅表单。
- build 通过 15 页；dev 服务在 9300（另有两个遗留 dev server 占用 9301/9302，无害）。

## 修正：图文分栏 + 错落 + 首页悬停高亮 + Resources 瀑布流
用户纠正上一轮理解偏差：Get Started 不是放文字一侧，而是「图片一侧 + 标题文字另一侧 + 按钮在文字下」，且 section 间错落交替。
- **services.astro / es/services.astro**：服务区彻底改为「图文左右分栏」编辑式长卷。每个 `.svc-row` 现在 = `.svc-row__media`(ImgPlaceholder 4/3) + `.svc-row__content`(indexnum + h3 + desc + details 清单 + Get Started 在底部 `align-self:flex-start`)；`i%2===1` 加 `.svc-row--reverse` 让图片/文字左右交替（错落）。删掉旧的 `.svc-row__meta`/`.lede-row` 用法。`.svc-row--featured` 不再用整块深绿底，改为仅 `.tag tag-seal`「Most requested / Más solicitado」角标 + 标题 `--seal` 红字（轻量强调，不破坏交替节奏）；btn-primary/outline 保留。移动端单列，media 在上 content 在下。
- **index.astro / es/index.astro 的 What We Do**：去掉静态 `.svc-row--featured` 深绿底与「Most requested / Más solicitado」角标。改为**悬停轮流高亮**：`.svc-row:hover{background:var(--avo-950)}` 并联动 h3/p/indexnum/链接转白/红，配 `transition`。默认所有行平等，鼠标移到哪个哪个高亮（满足「轮流重点显示」）。作用域样式已进构建产物。
- **blog/index.astro（Resources）**：`.posts` 由 `grid repeat(3,1fr)` 改为 **CSS columns 瀑布流**（`column-count:3` / 平板 `2` / 手机 `1`，`column-gap` 同 `--s5`）；`.pcard` 改 `display:block;break-inside:avoid;margin-bottom`，图片 `width:100%`；并按索引给 5 篇文章分配不同图片比例 `['4/3','3/4','1/1','2/1','4/5']` 形成高低错落，消除大段空白。筛选脚本 `display none/''` 在 column 布局下仍正常重排。
- build 通过 15 页；产物已校验（services 含 media/content/reverse、blog 含 column-count:3、首页无静态 Most requested、悬停规则存在）。

## 全站图片替换为采购代理相关 AI 图
用户要求把全部图片换成与采购代理相关的图。原图全为 picsum.photos 随机占位（ImgPlaceholder 组件不传 src 时显示）。用 ImageGen（独立图像模型，约 18 张 × 5-10 credits ≈ 150 credits）生成实景摄影风图，落地 public/img/（共 20 张 png = 18 生成 + 2 logo）。
- **生成图清单（18 张）**：hero-warehouse(竖向仓库)、sourcing-1688(义乌/1688采购现场)、inspection(工厂质检)、payment(合同付款)、shipping(港口集装箱)、factory(验厂生产线)、reorder(库存补货)、team-wide(团队办公室)、team-wei/team-amara/team-carlos(3 成员人像：亚洲男采购员/黑人女客户成功/拉美男物流专家)、g-yiwu/g-production/g-packing/g-unboxing(义乌市场/生产线/装箱/开箱)、blog-products(热销商品)、blog-safety(防骗合同)、blog-packaging(定制包装)。提示词全英文、no text/no logos（国际化无中文）。
- **复用映射**（省生成）：blog 的 1688/货运/付款三篇复用 sourcing-1688/shipping/payment；首页 What We Do 4 类复用 services 同款；首页 Why 团队图与 about 大图复用 team-wide。实际唯一图 18 张覆盖全站 21 个语义位置（en/es 镜像共用同一本地文件）。
- **代码改动**：8 个页面文件各加 `seed→文件名` 映射 const（svcImg/blogImg/teamImg/galImg），并把所有 ImgPlaceholder 调用补 `src={'/img/'+map[seed]+'.png'}`、删占位 hint。services/es.services(6)、about/es.about(团队大图+3人像+4gallery)、index/es.index(Hero+WhatWeDo缩略图+Why团队+博客精选/小图)、blog/index(精选+列表)、blog/[slug](6篇头图)。
- **坑**：① Astro 文件 CRLF，多行 old_string 用 \n 不匹配，改用单行锚点;② es/index 的 svc-row__thumb 缩略图首轮漏改（仍 picsum），已补;③ blog/[slug] 头图属性跨行，src 加在 seed 行。
- **验证**：build 15 页通过；dist picsum 残留 0，/img/ 引用 88 处。注意：about 页团队成员为 AI 生成的代表性商务人像（非真实员工），交付时已注明，建议后续替换为真实团队照。
