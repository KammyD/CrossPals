---
import BaseLayout from '../../layouts/BaseLayout.astro';
import ImgPlaceholder from '../../components/ImgPlaceholder.astro';
import Icon from '../../components/Icon.astro';
import type { IconName } from '../../components/Icon.astro';

const services: Array<{ icon: IconName; n: string; title: string; body: string; seed: number; hint: string; delay: number; featured?: boolean }> = [
  { icon: 'sourcing',   n: '01', title: 'Búsqueda de Productos', body: 'Buscamos entre miles de proveedores chinos para encontrar la mejor opción según tu presupuesto y estándares de calidad.', seed: 10, hint: '义乌商品城/供应商展会', delay: 0 },
  { icon: 'inspection', n: '02', title: 'Inspección de Calidad', body: 'Inspección física en fábrica antes del envío. Informe fotográfico y veredicto claro antes de aprobar el embarque.', seed: 20, hint: '质检员工作照/工厂', delay: 1, featured: true },
  { icon: 'payment',    n: '03', title: 'Compra y Pago',         body: 'Negociamos en chino y gestionamos todos los pagos a proveedores verificados. Tu dinero está protegido en cada paso.', seed: 30, hint: '签约/握手/合同', delay: 2 },
  { icon: 'shipping',   n: '04', title: 'Envío y Logística',     body: 'Entrega puerta a puerta en todo el mundo. Comparamos opciones de flete y gestionamos la documentación aduanera.', seed: 40, hint: '集装箱港口/货运', delay: 3 },
];

const steps: Array<{ day: string; title: string; body: string }> = [
  { day: 'Día 1',    title: 'Envías tu solicitud',      body: 'Cuéntanos el tipo de producto, cantidad, requisitos y presupuesto en nuestro formulario.' },
  { day: 'Día 2',    title: 'Opciones de proveedores',  body: '3–5 proveedores verificados con precios y perfiles, en tu bandeja en 48 horas.' },
  { day: 'Día 7–20', title: 'Compramos e inspeccionamos', body: 'Pagamos al proveedor, supervisamos la producción y te enviamos el informe QC antes del envío.' },
  { day: 'Día 21+',  title: 'Entregado a tu puerta',    body: 'Los productos salen de China con número de rastreo. Aduanas gestionadas. Tú solo esperas.' },
];

const whyItems: Array<{ icon: IconName; n: string; title: string; body: string }> = [
  { icon: 'global',       n: '01', title: 'Sin Barrera de Idioma', body: 'Negociamos directamente en chino — obtienes precios de fábrica, no de turista.' },
  { icon: 'trust',        n: '02', title: 'Responsabilidad Real',  body: 'Un agente dedicado gestiona tu pedido. Una persona, no una cola de tickets.' },
  { icon: 'handshake',    n: '03', title: 'Hecho para Principiantes', body: '¿Nunca has importado? Te guiamos paso a paso, de la primera consulta a la primera venta.' },
  { icon: 'transparent',  n: '04', title: 'Costos Transparentes',  body: 'Precio de proveedor + envío + nuestra tarifa. Ves todo antes de comprometerte.' },
];

const blogPosts = [
  { cat: 'Guía de Abastecimiento', title: 'How to Find Reliable Suppliers on 1688 Without Speaking Chinese', slug: 'find-suppliers-1688', time: '5 min', seed: 101, hint: '1688/电商' },
  { cat: 'Envío',                 title: 'Air vs Sea Freight: Which to Choose for Your First Order?',        slug: 'air-vs-sea-freight',  time: '4 min', seed: 102, hint: '货运飞机/码头' },
  { cat: 'Tips de Negocio',       title: '5 Products That Are Cheaper in China and Sell Well Everywhere',    slug: 'cheap-china-products', time: '6 min', seed: 103, hint: '热销商品' },
];

const tickerItems = ['Lagos','São Paulo','Ciudad de México','Manila','Nairobi','Bogotá','El Cairo','Yiwu → Mundo','Búsqueda 1688','Inspeccionado QC','Envío DDP','Proveedores Verificados'];
---
<BaseLayout title="Abastecimiento Confiable desde China para Emprendedores" description="CrossPals conecta emprendedores globales con proveedores chinos de confianza." lang="es">

<div class="masthead">
  <div class="container masthead__inner">
    <span>CrossPals · Manual de Abastecimiento</span>
    <span class="masthead__rule" aria-hidden="true"></span>
    <span>Edición Nº01 — MMXXVI</span>
  </div>
</div>

<section class="hero grain" style="position:relative;overflow:hidden;">
  <div class="hero__grid">
    <div class="hero__left">
      <div class="hero__left-inner">
        <p class="overline overline--light">Tu Socio de Abastecimiento en China</p>
        <h1 class="hero__title">Eliminamos el <em>riesgo</em> de comprar en China.</h1>
        <p class="hero__lead">CrossPals es el agente dedicado entre tu idea y la fábrica — búsqueda, inspección, pago y envío de principio a fin, para que nunca transfieras a un desconocido ni adivines la calidad.</p>
        <p class="hero__sidenote">Fig. 00 — La mayoría de los primeros importadores pierde dinero por barreras idiomáticas, proveedores falsos y envíos a ciegas. Nosotros cerramos las tres.</p>
        <div class="hero__actions">
          <a href="/es/contact" class="btn btn-primary">Cotización Gratis <svg class="arrow-icon" viewBox="0 0 16 16" fill="none" stroke="currentColor" stroke-width="2.5"><path d="M3 8h10M8 3l5 5-5 5"/></svg></a>
          <a href="/es/services" class="btn btn-ghost">Cómo Funciona</a>
        </div>
        <ul class="hero__index">
          <li><span>01</span> Búsqueda</li>
          <li><span>02</span> Inspección</li>
          <li><span>03</span> Pago</li>
          <li><span>04</span> Envío</li>
        </ul>
      </div>
    </div>
    <div class="hero__right">
      <div class="hero__photo-fill">
        <ImgPlaceholder seed={42} width={900} height={1100} ratio="9/11" alt="Almacén en China — CrossPals" hint="工厂车间/仓库/产品特写" />
      </div>
      <div class="hero__cap">
        <span class="hero__cap-label">Fig. 01</span>
        <span class="hero__cap-text">Mercado Internacional de Yiwu, Zhejiang</span>
        <span class="seal seal--sm" aria-hidden="true"><span>Verified</span><span>Agent</span></span>
      </div>
    </div>
  </div>
</section>

<div class="ticker" aria-hidden="true">
  <div class="ticker__track">
    {[...tickerItems, ...tickerItems].map(t => <span class="ticker__item">{t}</span>)}
  </div>
</div>

<section class="section svc ledger">
  <div class="container svc__wrap">
    <aside class="svc__aside">
      <p class="overline">Qué Hacemos</p>
      <h2 class="svc__h">Abastecimiento<br/><em>desde China</em></h2>
      <p class="svc__intro">Cuatro disciplinas, un agente responsable. Cada paso se documenta, fotografía y aprueba antes de que tu dinero se mueva.</p>
      <a href="/es/services" class="svc__all">Índice de servicios →</a>
    </aside>
    <ol class="svc__list">
      {services.map(s => (
        <li class:list={['svc-row reveal', {'svc-row--featured': s.featured}, `reveal-delay-${s.delay}`]}>
          <div class="svc-row__num"><span class="indexnum">{s.n}</span></div>
          <div class="svc-row__thumb"><ImgPlaceholder seed={s.seed} width={360} height={240} ratio="3/2" alt={s.title} hint={s.hint} /></div>
          <div class="svc-row__body">
            <div class="svc-row__top">
              <h3>{s.title}</h3>
              {s.featured && <span class="tag tag-seal">Más solicitado</span>}
            </div>
            <p>{s.body}</p>
            <a href="/es/services" class="svc-row__link">Más información <svg width="13" height="13" viewBox="0 0 16 16" fill="none" stroke="currentColor" stroke-width="2.5"><path d="M3 8h10M8 3l5 5-5 5"/></svg></a>
          </div>
        </li>
      ))}
    </ol>
  </div>
</section>

<section class="section hiw grain" style="position:relative;overflow:hidden;">
  <div class="container">
    <div class="hiw__head reveal">
      <p class="overline overline--light">Proceso</p>
      <h2 class="text-white">De la Idea al Inventario<br/><em style="color:var(--avo-300)">en 4 Pasos</em></h2>
    </div>
    <div class="manifest reveal reveal-delay-1">
      {steps.map((step, i) => (
        <div class:list={['manifest__col', { 'manifest__col--end': i === steps.length - 1 }]}>
          <span class="manifest__day">{step.day}</span>
          <span class="manifest__n">{String(i + 1).padStart(2, '0')}</span>
          <h4 class="manifest__title">{step.title}</h4>
          <p class="manifest__body">{step.body}</p>
        </div>
      ))}
      <div class="manifest__stamp" aria-hidden="true"><span class="seal"><span>Cross</span><span>Pals ✓</span></span></div>
    </div>
    <div class="hiw__cta reveal reveal-delay-2">
      <a href="/es/contact" class="btn btn-primary">Empieza tu Primer Pedido</a>
      <a href="/es/about" class="btn btn-ghost">Conoce al Equipo</a>
    </div>
  </div>
</section>

<section class="section testi">
  <div class="container">
    <div class="testi__head reveal">
      <p class="overline">Casos de Éxito</p>
      <h2>Negocios Reales,<br/><em>Resultados Reales</em></h2>
    </div>
    <blockquote class="pullquote reveal">
      <span class="pullquote__mark" aria-hidden="true">“</span>
      <p>CrossPals encontró un proveedor certificado, gestionó 3 envíos en 6 meses, y ahora tengo una marca real — todo comenzando con $800.</p>
      <footer><span class="pullquote__region">🇧🇷 Brasil · São Paulo</span><span class="pullquote__result">ROI 3× en primer trimestre</span></footer>
    </blockquote>
    <div class="testi__row">
      <blockquote class="pq-sm reveal reveal-delay-1">
        <p>"Mi proveedor en Yiwu en 48 horas. Fotos QC, envío DHL — nunca salí de Lagos."</p>
        <footer><span>🇳🇬 Nigeria</span><span class="text-seal">40% menos costo</span></footer>
      </blockquote>
      <blockquote class="pq-sm reveal reveal-delay-2">
        <p>"El soporte de reorden continuo significa que siempre soy competitiva. 12 pedidos y contando."</p>
        <footer><span>🇲🇽 México</span><span class="text-seal">12 reordenes y creciendo</span></footer>
      </blockquote>
      <div class="testi__stat reveal reveal-delay-1">
        <p class="overline overline--avo">Nuestro historial</p>
        <div class="testi__bignum">500<span>+</span></div>
        <p>emprendedores atendidos en 30+ países de África, América Latina y el Sudeste Asiático.</p>
        <a href="/es/about" class="btn btn-outline" style="margin-top:var(--s5);border-color:rgba(186,209,133,.4);color:rgba(255,255,255,.85);">Nuestra Historia →</a>
      </div>
    </div>
  </div>
</section>

<section class="section why bg-parchment">
  <div class="container why__grid">
    <div class="why__left reveal">
      <p class="overline">Por Qué CrossPals</p>
      <h2>Resolvimos las Partes<br/><em>Difíciles para Ti</em></h2>
      <div class="divider" style="margin:var(--s6) 0;"></div>
      <p style="font-size:16px;line-height:1.8;">La mayoría fracasa en el abastecimiento desde China por barreras idiomáticas, riesgo de pago e incertidumbre de calidad. CrossPals elimina las tres.</p>
      <ImgPlaceholder seed={15} width={560} height={420} ratio="4/3" alt="Equipo CrossPals" hint="团队合影/中国市场" />
      <a href="/es/contact" class="btn btn-primary" style="margin-top:var(--s6);">Empieza Gratis <svg class="arrow-icon" viewBox="0 0 16 16" fill="none" stroke="currentColor" stroke-width="2.5"><path d="M3 8h10M8 3l5 5-5 5"/></svg></a>
    </div>
    <div class="why__right">
      {whyItems.map((w, i) => (
        <div class:list={['why-item reveal', `reveal-delay-${i}`]}>
          <span class="indexnum why-item__n">{w.n}</span>
          <div class="why-item__body">
            <h4>{w.title}</h4>
            <p>{w.body}</p>
            <p class="sidenote why-item__note">Estándar CrossPals — aplica a cada pedido, no solo a los grandes.</p>
          </div>
        </div>
      ))}
    </div>
  </div>
</section>

<section class="section blog-prev">
  <div class="container">
    <div class="blog-prev__head reveal">
      <div><p class="overline">Recursos</p><h2>Base de<br/><em>Conocimiento</em></h2></div>
      <a href="/blog" class="btn btn-outline">Todos los Artículos →</a>
    </div>
    <div class="blog-prev__grid">
      <a href={`/blog/${blogPosts[0].slug}`} class="bcard bcard--feat reveal">
        <ImgPlaceholder seed={blogPosts[0].seed} width={720} height={460} ratio="16/10" alt={blogPosts[0].title} hint={blogPosts[0].hint} />
        <div class="bcard__body">
          <span class="bcard__cat">{blogPosts[0].cat}</span>
          <h3>{blogPosts[0].title}</h3>
          <div class="bcard__foot">
            <div class="bcard__time"><Icon name="clock" size={12} color="var(--warm-400)" /><span>{blogPosts[0].time} lectura</span></div>
            <svg width="13" height="13" viewBox="0 0 16 16" fill="none" stroke="currentColor" stroke-width="2.5"><path d="M3 8h10M8 3l5 5-5 5"/></svg>
          </div>
        </div>
      </a>
      <div class="blog-prev__side">
        {blogPosts.slice(1).map((post, i) => (
          <a href={`/blog/${post.slug}`} class:list={['bcard bcard--sm reveal', `reveal-delay-${i + 1}`]}>
            <ImgPlaceholder seed={post.seed} width={320} height={200} ratio="16/10" alt={post.title} hint={post.hint} />
            <div class="bcard__body">
              <span class="bcard__cat">{post.cat}</span>
              <h4>{post.title}</h4>
              <div class="bcard__foot">
                <div class="bcard__time"><Icon name="clock" size={12} color="var(--warm-400)" /><span>{post.time} lectura</span></div>
                <svg width="13" height="13" viewBox="0 0 16 16" fill="none" stroke="currentColor" stroke-width="2.5"><path d="M3 8h10M8 3l5 5-5 5"/></svg>
              </div>
            </div>
          </a>
        ))}
      </div>
    </div>
  </div>
</section>

<section class="cta-final grain" style="position:relative;overflow:hidden;">
  <div class="container cta-final__inner">
    <div class="cta-final__text reveal">
      <span class="seal cta-final__seal" aria-hidden="true"><span>Listo</span><span>48h</span></span>
      <p class="overline overline--light">¿Listo?</p>
      <h2 class="text-white" style="margin:var(--s3) 0 var(--s4);font-size:clamp(30px,4.4vw,56px);">Tu Primer Proveedor<br/><em style="color:var(--avo-300)">Está a 48 Horas</em></h2>
      <p class="text-light" style="max-width:400px;">Sin compromiso. Sin experiencia. Solo dinos qué quieres importar.</p>
    </div>
    <div class="cta-final__form reveal reveal-delay-2">
      <form class="quick-form" action="/es/contact" method="GET">
        <input type="text" name="product" placeholder="¿Qué quieres importar?" required />
        <button type="submit" class="btn btn-primary">Cotizar <svg class="arrow-icon" viewBox="0 0 16 16" fill="none" stroke="currentColor" stroke-width="2.5"><path d="M3 8h10M8 3l5 5-5 5"/></svg></button>
      </form>
      <p class="cta-final__note">O <a href="https://wa.me/yourwhatsapp">chatea por WhatsApp</a> · Gratis, sin compromiso</p>
    </div>
  </div>
</section>

</BaseLayout>

<style>
/* — Home styles shared with EN (scoped copy) — */
.masthead{background:var(--avo-950);border-bottom:1px solid rgba(255,255,255,.08);}
.masthead__inner{display:flex;align-items:center;gap:var(--s4);padding:10px var(--s5);font-family:var(--font-mono);font-size:11px;letter-spacing:.12em;text-transform:uppercase;color:rgba(255,255,255,.5);}
.masthead__rule{flex:1;height:1px;background:rgba(255,255,255,.1);}
.hero{background:var(--avo-950);min-height:100vh;display:flex;flex-direction:column;}
.hero__grid{display:grid;grid-template-columns:1.05fr 0.95fr;flex:1;min-height:0;}
.hero__left{display:flex;align-items:center;padding:120px var(--s8) var(--s8) var(--s7);}
.hero__left-inner{max-width:560px;}
.hero__title{color:#fff;font-size:clamp(40px,5.4vw,72px);line-height:1.04;margin:var(--s3) 0 var(--s5);font-weight:560;}
.hero__title em{font-style:italic;color:var(--seal);font-weight:400;}
.hero__lead{color:rgba(255,255,255,.62);font-size:17px;line-height:1.75;max-width:460px;margin-bottom:var(--s5);}
.hero__sidenote{font-family:var(--font-mono);font-size:11px;line-height:1.6;color:rgba(255,255,255,.4);border-left:1px solid var(--seal);padding-left:var(--s3);max-width:420px;margin-bottom:var(--s7);}
.hero__actions{display:flex;gap:var(--s3);flex-wrap:wrap;margin-bottom:var(--s7);}
.hero__index{list-style:none;display:flex;flex-wrap:wrap;gap:var(--s5);padding-top:var(--s5);border-top:1px solid rgba(255,255,255,.1);}
.hero__index li{display:flex;align-items:baseline;gap:6px;font-family:var(--font-serif);font-size:15px;color:rgba(255,255,255,.7);}
.hero__index li span{font-family:var(--font-mono);font-size:10px;color:var(--seal);letter-spacing:.08em;}
.hero__right{position:relative;overflow:hidden;display:flex;}
.hero__photo-fill{position:absolute;inset:0;}
.hero__photo-fill :global(.img-ph){height:100%;}
.hero__photo-fill :global(.img-ph__wrap){padding-top:0!important;height:100%;}
.hero__photo-fill :global(img){width:100%;height:100%;object-fit:cover;}
.hero__cap{position:absolute;left:0;right:0;bottom:0;display:flex;align-items:center;gap:var(--s4);padding:var(--s4) var(--s5);background:linear-gradient(0deg,rgba(28,33,20,.9),transparent);}
.hero__cap-label{font-family:var(--font-mono);font-size:11px;letter-spacing:.1em;color:var(--avo-300);}
.hero__cap-text{font-family:var(--font-mono);font-size:11px;letter-spacing:.06em;color:rgba(255,255,255,.7);text-transform:uppercase;flex:1;}
.hero__cap .seal{transform:rotate(-8deg);background:rgba(28,33,20,.4);border-color:var(--seal);color:#fff;backdrop-filter:blur(2px);}
.svc{background:var(--bg-page);}
.svc__wrap{display:grid;grid-template-columns:300px 1fr;gap:var(--s9);align-items:start;}
.svc__aside{position:sticky;top:100px;}
.svc__h em{font-style:italic;color:var(--avo-600);}
.svc__intro{font-size:15px;line-height:1.75;color:var(--text-body);margin:var(--s5) 0;}
.svc__all{font-family:var(--font-mono);font-size:12px;letter-spacing:.04em;color:var(--seal);text-transform:uppercase;}
.svc__list{list-style:none;display:flex;flex-direction:column;}
.svc-row{display:grid;grid-template-columns:64px 200px 1fr;gap:var(--s5);align-items:center;padding:var(--s6) 0;border-top:1px solid var(--border-light);}
.svc-row:last-child{border-bottom:1px solid var(--border-light);}
.svc-row__num .indexnum{font-size:48px;}
.svc-row__thumb :global(.img-ph){border:1px solid var(--border-light);}
.svc-row__top{display:flex;align-items:center;gap:var(--s3);margin-bottom:var(--s2);flex-wrap:wrap;}
.svc-row__body h3{font-size:clamp(20px,2.4vw,28px);}
.svc-row__body p{font-size:14px;line-height:1.7;margin:var(--s2) 0 var(--s4);max-width:520px;}
.svc-row__link{display:inline-flex;align-items:center;gap:6px;font-size:13px;font-weight:600;color:var(--seal);transition:gap var(--t-fast);}
.svc-row__link:hover{gap:10px;}
.svc-row--featured{background:var(--avo-950);padding:var(--s6);margin:var(--s4) 0;border-top:none;border-bottom:none;}
.svc-row--featured + .svc-row{border-top:none;}
.svc-row--featured .svc-row__body h3{color:#fff;}
.svc-row--featured .svc-row__body p{color:rgba(255,255,255,.62);}
.svc-row--featured .svc-row__num .indexnum{color:var(--avo-300);}
.svc-row--featured .svc-row__thumb :global(.img-ph){border-color:rgba(255,255,255,.15);}
.hiw{background:var(--avo-950);}
.hiw__head{margin-bottom:var(--s8);}
.hiw__head h2 em{font-style:italic;color:var(--avo-300);}
.manifest{display:grid;grid-template-columns:repeat(4,1fr);border-top:1px solid rgba(255,255,255,.15);position:relative;}
.manifest__col{padding:var(--s6) var(--s5) var(--s7) 0;border-right:1px solid rgba(255,255,255,.08);}
.manifest__day{display:block;font-family:var(--font-mono);font-size:11px;letter-spacing:.1em;text-transform:uppercase;color:var(--seal);margin-bottom:var(--s4);}
.manifest__n{display:block;font-family:var(--font-serif);font-size:64px;font-weight:600;color:rgba(255,255,255,.1);line-height:1;margin-bottom:var(--s3);}
.manifest__title{color:#fff;font-size:19px;margin-bottom:var(--s3);}
.manifest__body{font-size:14px;color:rgba(255,255,255,.5);}
.manifest__stamp{position:absolute;right:0;top:-10px;}
.hiw__cta{display:flex;gap:var(--s4);flex-wrap:wrap;margin-top:var(--s8);}
.testi{background:var(--warm-100);}
.testi__head{margin-bottom:var(--s7);}
.testi__head h2 em{font-style:italic;color:var(--avo-600);}
.pullquote{position:relative;margin:0 0 var(--s7);padding:var(--s5) 0 var(--s5) var(--s7);border-left:2px solid var(--seal);}
.pullquote__mark{position:absolute;left:-6px;top:-26px;font-family:var(--font-serif);font-size:96px;line-height:1;color:var(--seal);opacity:.25;}
.pullquote p{font-family:var(--font-serif);font-style:italic;font-size:clamp(22px,3vw,34px);line-height:1.4;color:var(--text-primary);max-width:820px;}
.pullquote footer{display:flex;align-items:center;gap:var(--s5);margin-top:var(--s5);flex-wrap:wrap;}
.pullquote__region{font-family:var(--font-mono);font-size:12px;letter-spacing:.06em;text-transform:uppercase;color:var(--text-muted);}
.pullquote__result{font-family:var(--font-mono);font-size:12px;font-weight:500;letter-spacing:.04em;color:var(--seal);}
.testi__row{display:grid;grid-template-columns:1fr 1fr 1fr;gap:var(--s5);}
.pq-sm{margin:0;padding:var(--s5) 0;border-top:1px solid var(--border-light);}
.pq-sm p{font-family:var(--font-serif);font-style:italic;font-size:17px;line-height:1.5;color:var(--text-primary);margin-bottom:var(--s4);}
.pq-sm footer{display:flex;flex-direction:column;gap:4px;}
.pq-sm footer span:first-child{font-family:var(--font-mono);font-size:12px;color:var(--text-muted);}
.pq-sm footer span:last-child{font-family:var(--font-mono);font-size:12px;font-weight:500;}
.testi__stat{background:var(--avo-950);padding:var(--s6);display:flex;flex-direction:column;}
.testi__stat p{color:rgba(255,255,255,.55);font-size:14px;}
.testi__bignum{font-family:var(--font-serif);font-size:64px;font-weight:600;color:#fff;line-height:1;margin:var(--s3) 0;}
.testi__bignum span{color:var(--avo-300);}
.why{background:var(--warm-100);}
.why__grid{display:grid;grid-template-columns:1fr 1.35fr;gap:var(--s10);align-items:start;}
.why h2 em{font-style:italic;color:var(--avo-600);}
.why__right{display:flex;flex-direction:column;}
.why-item{display:grid;grid-template-columns:72px 1fr;gap:var(--s5);padding:var(--s6) 0;border-bottom:1px solid var(--border-light);}
.why-item:first-child{padding-top:0;}
.why-item:last-child{border-bottom:none;}
.why-item__n{font-size:44px;}
.why-item__body h4{font-size:19px;margin-bottom:var(--s2);}
.why-item__body p{font-size:14px;}
.why-item__note{margin-top:var(--s3);}
.blog-prev{background:var(--bg-page);}
.blog-prev__head{display:flex;justify-content:space-between;align-items:flex-end;margin-bottom:var(--s8);}
.blog-prev__head h2 em{font-style:italic;color:var(--avo-600);}
.blog-prev__grid{display:grid;grid-template-columns:1.4fr 1fr;gap:var(--s6);}
.blog-prev__side{display:flex;flex-direction:column;gap:var(--s6);}
.bcard{display:flex;flex-direction:column;text-decoration:none;color:inherit;background:var(--bg-surface);border:1px solid var(--border-light);transition:var(--t-med);}
.bcard:hover{transform:translateY(-4px);box-shadow:var(--shadow-md);border-color:var(--avo-200);}
.bcard--feat :global(.img-ph){border-bottom:1px solid var(--border-light);}
.bcard__body{padding:var(--s5);display:flex;flex-direction:column;gap:var(--s3);flex:1;}
.bcard__cat{font-family:var(--font-mono);font-size:10px;font-weight:500;letter-spacing:.12em;text-transform:uppercase;color:var(--avo-600);}
.bcard--feat h3{font-size:clamp(22px,2.6vw,30px);line-height:1.25;flex:1;transition:color var(--t-fast);}
.bcard--feat:hover h3{color:var(--avo-700);}
.bcard--sm h4{font-size:17px;line-height:1.3;flex:1;}
.bcard__foot{display:flex;justify-content:space-between;align-items:center;padding-top:var(--s4);border-top:1px solid var(--border-light);margin-top:auto;}
.bcard__time{display:flex;align-items:center;gap:5px;font-size:11px;color:var(--warm-400);font-family:var(--font-mono);}
.cta-final{background:var(--avo-950);padding:var(--s9) var(--s5);position:relative;overflow:hidden;}
.cta-final__inner{display:flex;align-items:center;justify-content:space-between;gap:var(--s8);flex-wrap:wrap;}
.cta-final__text{position:relative;max-width:560px;}
.cta-final__seal{position:absolute;top:-30px;right:0;background:rgba(28,33,20,.5);border-color:var(--seal);color:#fff;}
.cta-final__form{display:flex;flex-direction:column;gap:var(--s3);min-width:360px;}
.quick-form{display:flex;gap:var(--s3);}
.quick-form input{flex:1;padding:13px 18px;border:1.5px solid rgba(255,255,255,.14);background:rgba(255,255,255,.07);color:#fff;font-size:15px;font-family:var(--font-sans);outline:none;transition:border-color var(--t-fast);}
.quick-form input:focus{border-color:var(--seal);}
.quick-form input::placeholder{color:rgba(255,255,255,.32);}
.cta-final__note{font-size:12px;color:rgba(255,255,255,.32);text-align:center;font-family:var(--font-mono);}
.cta-final__note a{color:var(--avo-300);text-decoration:underline;}
@media(max-width:1100px){
  .svc__wrap{grid-template-columns:1fr;gap:var(--s7);}
  .svc__aside{position:static;}
  .manifest{grid-template-columns:1fr 1fr;}
  .manifest__col{border-right:none;border-bottom:1px solid rgba(255,255,255,.08);}
  .manifest__stamp{display:none;}
  .testi__row{grid-template-columns:1fr;}
  .blog-prev__grid{grid-template-columns:1fr;}
}
@media(max-width:860px){
  .hero__grid{grid-template-columns:1fr;}
  .hero__right{display:none;}
  .hero__left{padding:120px var(--s5) var(--s7);}
}
@media(max-width:640px){
  .svc-row{grid-template-columns:48px 1fr;gap:var(--s4);}
  .svc-row__thumb{display:none;}
  .manifest{grid-template-columns:1fr;}
  .cta-final__inner{flex-direction:column;text-align:center;}
  .cta-final__form{min-width:unset;width:100%;}
  .quick-form{flex-direction:column;}
}
</style>
